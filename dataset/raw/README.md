# EarthFire raw dataset

`EarthFire_MODIS_FIRMS_combined.zip` contains the user-provided FIRMS MODIS CSV (758,096 records). The CSV SHA-256 is `6b92bb6d840e76181e195420b14628ba24766227fdd896298da58f3eec347bee`. Archive and NRT rows are distinguished by `data_source` and must be processed separately.
